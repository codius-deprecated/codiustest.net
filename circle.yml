dependencies:
  pre:
    - sudo pip install awscli boto
test:
  override:
    - echo "Its just a static page!"
deployment:
  s3:
    branch: master
    commands:
      - aws s3 sync `pwd` s3://codiustest.net/
